% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_utils.R
\name{impute_h2o_drf_fit_apply}
\alias{impute_h2o_drf_fit_apply}
\title{Impute with H2O Distributed Random Forest (single-session fit & apply)}
\usage{
impute_h2o_drf_fit_apply(
  df,
  cols = NULL,
  exclude_predictors = NULL,
  train_rows = NULL,
  ntrees = 50,
  max_depth = 10,
  min_rows = 5,
  sample_rate = 0.8,
  col_sample_rate_per_tree = 0.8,
  balance_classes = FALSE,
  seed = 1,
  round_digits = NA,
  h2o_threads = NULL,
  h2o_mem = NULL,
  h2o_port = "auto",
  h2o_connection = NULL,
  nfolds = 0,
  fold_assignment = "AUTO",
  verbose = TRUE,
  progress_hook = NULL
)
}
\arguments{
\item{df}{Data frame.}

\item{cols}{Optional target columns (default: auto-detect with missingness).}

\item{exclude_predictors}{Optional predictors to exclude from modeling.}

\item{train_rows}{Optional integer indices used for model training.}

\item{ntrees, max_depth, min_rows, sample_rate, col_sample_rate_per_tree}{DRF params.}

\item{balance_classes}{Logical; whether to balance classes for classification.}

\item{seed}{Random seed.}

\item{round_digits}{Optional integer; round numeric columns post-imputation.}

\item{h2o_threads, h2o_mem, h2o_port, h2o_connection}{H2O cluster settings.}

\item{verbose}{Logical; emit progress messages.}

\item{progress_hook}{Optional callback invoked per target column.}
}
\value{
Data frame with imputed values.
}
\description{
For each target column with missingness, trains a DRF on observed rows and
predicts missing entries, handling both numeric and factor targets. Optionally
rounds numeric columns at the end.
}
\seealso{
h2o::h2o.randomForest
}
